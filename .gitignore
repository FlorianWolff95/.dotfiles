# Hyprland regenerates a stub hyprland.conf if a running (pre-0.55) session finds
# its config missing. The real config is hyprland.lua; the stub is autogenerated
# garbage and must never be committed.
hyprland/.config/hypr/hyprland.conf
